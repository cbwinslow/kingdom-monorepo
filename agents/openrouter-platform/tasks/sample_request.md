# Sample autonomous task

- Goal: Stand up a shell-first agent toolkit that syncs dotfiles and exposes a multi-agent runner.
- Deliverables: architecture overview, runnable payload, QA checklist.
- Constraints: prefer OpenRouter free/low-cost models, avoid long-lived state when not using Postgres.
