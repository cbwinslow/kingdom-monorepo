# Redis Configuration
REDIS_PASSWORD=change_this_redis_password

# Logging
LOG_LEVEL=info

# Session Secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change_this_to_a_random_string_generated_by_openssl
